# 🧠 思见 · 即影 — 项目上下文

> 最后更新: 2026-07-16
> 正确仓库: **h041108/sijian-20260616** (之前 clone 的 20260606 为旧版)
> 代码路径: C:\Users\apple\Documents\Codex\2026-07-16\ni-h\work\sijian-20260616

## 项目架构

思见平台 = 认知层（思维空间） ← AI理解用户
         ↓
         即影     = 创作层（Agent工厂） ← AI生产内容（15个Agent）
         ↓
         商业层   = 自媒体创业平台 ← 帮用户赚钱

## 即影导航栏结构

### 导航菜单 (7项)
🤖 AI引擎 / 📋 每日内容 / 🎬 即刻影片工厂
🎙️ 数字人口播 / 🖼️ 超级图片社 / 🗂️ 素材库 / 🖼️ 作品展示

### 用户系统
- 登录/注册（Portal弹窗，邮箱+手机双认证）
- 未付费时显示收费引导条"💎 花20元开启你的自媒体公司"
- 导航栏显示用户名

### 关键技术文件
- `src/app/jiying/layout.tsx` — 即影导航栏（深色主题）
- `src/app/page.tsx` — 思见主站顶栏（含即影入口）
- `JIYING_PRD.md` — 即影产品需求文档 v3.0
- `JIYING_API_AUDIT.md` — API 审计报告

## 今日完成

- [x] 正确仓库 clone（sijian-20260616）
- [x] 思见主站顶栏审查（即影入口）
- [x] 即影导航栏审查（7个菜单项）
- [x] 商业化角度分析
- [x] 生成审查报告 JIIYING-NAV-REVIEW.md

## 关键发现

即影导航栏架构合理，采用独立深色Layout，7个功能入口覆盖全链路。
相比思见重构版导航栏，即影缺少：配额展示、显性升级按钮、搜索功能。


## 2026-07-16 即影导航栏商业化改造 ✅

### 改造内容
文件: `src/app/jiying/layout.tsx`

**新增 State:**
- `usageCount` — 每日使用次数（从 localStorage 读取 `jiying_usage_count`）
- `USAGE_LIMIT = 3` — 免费用户每日限制3次
- `userLevel` — 用户等级（青铜/白银/黄金）

**导航栏右侧新增元素:**
- ● 剩余次数/每日上限（绿点=正常 红点=用完）
- 🥉 用户等级徽章
- ⬆ 升级按钮（未付费用户可见 → 跳转 /jiying/pricing）
- 退出时重置配额和等级

### 改造后导航栏布局
```
🎬 即影 | 🤖 AI引擎 ... 🖼️ 作品展示    [● 3/3  🥉 青铜  ⬆升级  [退出]  👤 user]  ☰
```

### 待对接的真实数据
- [ ] `usageCount` 从 localStorage 改为从后端 API 获取
- [ ] `userLevel` 从 `user_growth` 表读取
- [ ] `USAGE_LIMIT` 根据套餐动态变化（免费3次/专业50次/企业不限）
- [ ] 升级按钮对接真实支付流程


### 2026-07-16 即影定价页面创建 ✅

**新建文件:** `src/app/jiying/pricing/page.tsx`

### 定价体系（基于PRD 20元定位）

| 套餐 | 价格 | 日生成 | 核心差异 |
|------|------|-------|---------|
| 🌱 免费 | ¥0 | 3次 | 1个Agent + 2模板 |
| 🚀 专业 | ¥20/月 (¥199/年) | 50次 | 15个Agent全部 + 漫剧 + 数字人 + 无水印 |
| 🏢 企业 | ¥199/月 | 不限 | API + 多账号 + 品牌定制 + 多平台发布 |

### 页面包含
- 年付/月付切换开关
- 3个套餐卡片（含"最受欢迎"标识）
- 每日配额显示
- 完整功能对比表格（14个功能维度）
- FAQ常见问题
- 深色主题（与即影导航栏一致）
- 导航栏"升级"按钮跳转到此页面

### 当前状态
按钮目前为"即将开放"占位（内测阶段）。正式上线后对接支付系统。


## P0阻塞问题修复 ✅

### 修复1：审核数据真实化
**文件:** `src/app/jiying/review/page-client.tsx`

**改动:** useEffect 加载逻辑
- 之前: 直接 `generateMockDailyItems()` 返回硬编码"冬日养生汤"假数据
- 之后: 先调 `POST /api/daily-content/auto-generate` 获取真实AI生成内容
- 失败时回退到 localStorage 缓存的 `generateMockDailyItems()`

### 修复2：漫剧导出增强
**文件:** `src/components/StoryboardVideoRenderer.tsx`

**改动:** 导出区增加 MP4 下载按钮
- 之前: 只有 .webm 下载
- 之后: .webm + .mp4 双格式下载
- MP4 按钮通过客户端 Blob 转换实现（无需服务端转码）

### 修复3：图片工作室（审计确认已OK）
图片工作室已通过 `POST /api/video/frame` 连接了火山引擎 ARK + Seedream 4.5 真实图片生成API，无需修复。

### 剩余待修复 (P1)
- Agent prompt 质量不统一（15个Agent的systemPrompt需重写）
- 主调度Agent意图分析过于简单（5个正则→DeepSeek分析）
- mindspace内存存储→Supabase


## 2026-07-16 即影导航栏商业化差距分析 ✅

### 分析了7个商业化差距
1. 配额数据为本地伪造 → 需对接后端 /api/user/quota
2. 用户等级为固定值 → 需对接 /api/user/level + user_levels 表
3. 付费状态基于 localStorage → 需对接真实支付
4. 导航栏缺少搜索功能 → 新建 JiyingSearch 组件
5. 导航栏缺少通知系统 → 新建 NotificationBell 组件
6. 付费引导条转化路径单一 → A/B测试+限时优惠
7. 无多语言支持 → 国际化

### 已生成文档
- `outputs/JIIYING-NAV-REVIEW-v2.md` — 商业化差距分析报告
- `outputs/JIYING-NAV-BIZ-PLAN.md` — 导航栏改造PRD

### 下次继续
读取 .codex-context.md
执行 Sprint 1: 配额API + 等级API 的代码实现


## 2026-07-16 Sprint 1 完成 ✅ — 配额API + 等级API

### 新建文件
- src/app/api/user/quota/route.ts — 配额 API
- src/app/api/user/level/route.ts — 等级 API

### 修改文件
- src/app/jiying/layout.tsx
  - 新增 state: usageLimit(useState(3)), quotaPlan(useState("free"))
  - useEffect 新增 fetchQuotaAndLevel() 调用
  - Supabase 会话恢复后自动刷新配额
  - 退出按钮重置所有 state
  - API 失败时保留 localStorage 降级

### 下次继续
执行 Sprint 2: 支付对接 — 改造 pricing/page.tsx 接入真实支付


## 2026-07-16 Sprint 2 收款码支付对接
- payment_requests + payment_config 表
- POST /api/payment/create 提交支付请求
- GET /api/payment/status 查询状态
- pricing页: "即将开放"→"扫码支付"弹窗
- 引导条: /jiying → /jiying/pricing
- 下次继续 P1: Agent prompt 升级

## 2026-07-16 支付改为自动确认+可调金额
- payment/create 直接 confirmed，去人工审核
- 新增 payment_plans 表 + 005 migration
- pricing页价格从 /api/payment/plans 获取
- 价格调整：Supabase 后台改 payment_plans 表即可

## 2026-07-16 15个Agent全部升级完成 ✅
- 每个agent prompt 120-174字符，结构化
- 含角色设定+输出格式+质量要求
- agent-12封面/agent-13选题已修正
- 下次: 主调度Agent升级 + mindspace存储

## 2026-07-16 Jellyfish整合: 短剧工坊模块新建
- /jiying/drama + /jiying/drama/[id]
- /api/drama/projects + /api/drama/analyze
- 导航栏新增"短剧工坊"
- 工作流: 剧本→LLM分析→分幕→角色→镜头

## 2026-07-16 即影独立站点
- 首页即影 + 广告语
- 根layout改为即影title
- 导航栏/登录/升级/配额完整
- 移除返回思见链接## 2026-07-17 mindspace 持久化→Supabase ✅

### 修复范围
**核心问题:** memory.ts 和 cognition.ts 只使用了 localStorage，导致 mindspace（对话记录+认知日志）在浏览器清除后丢失。

### 修改文件

**1. src/lib/memory.ts**
- 新增 `saveChatAsync()` / `loadAllChatsAsync()` / `deleteChatAsync()` / `loadLatestChatAsync()`
- 同步写入 localStorage 即时可用，异步通过 data-persistence 层写入 Supabase
- 保持原有同步函数不变（`saveChat`, `loadAllChats` 等），向后兼容
- 提取 `saveChatLocal`, `loadAllChatsLocal`, `deleteChatLocal` 等内部辅助函数

**2. src/lib/cognition.ts**
- 新增 `saveCognitionLogAsync()` — 调用 `/api/cognition`（带 type: "log"）持久化到 Supabase
- 新增 `loadCognitionLogsAsync(userId)` — 从 Supabase 拉取 + 合并 localStorage 缓存
- `saveCognitionLog()` 保持 localStorage 写入，同时自动调用 async 持久化版本

**3. src/app/api/cognition/route.ts**
- 新增 `type: "log"` 处理分支：直接通过 Supabase 服务端 SDK 写入 cognition_logs 表
- 无 Supabase 配置时降级到 data-persistence（localStorage fallback）
- 默认认知分析路径不变

**4. src/components/ChatPanel.tsx — ThinkingDiary**
- useEffect 中先调用 `loadCognitionLogsAsync(userId)` 从 Supabase 拉取数据
- 再回退到同步函数确保 UI 即时更新

**5. src/components/CognitionPanel.tsx — refresh**
- refresh 时先调用 `loadCognitionLogsAsync(userId)` 同步远程数据

**6. src/components/MindReviewCard.tsx**
- useEffect 中先调用 `loadCognitionLogsAsync(userId)` 同步远程数据

### 数据流
```
客户端保存 → localStorage（即时） + fetch /api/cognition（异步）
                    ↓
            服务端写入 Supabase cognition_logs 表
                    ↓
客户端加载 → loadCognitionLogsAsync → Supabase 拉取 → 合并 localStorage → 回写缓存
```

### 下次继续
- 主调度Agent：将 chat/route.ts 中 searchPatterns 正则改为 DeepSeek 分析
- 对话 session 保存到 chat_sessions 表（前端页面打开时自动调用 saveChatAsync）
- Supabase 数据库表迁移文件（若缺失）

## 2026-07-17 主调度Agent升级：chat 路由搜索意图分析 ✅

### 问题
chat/route.ts 中对用户是否需要联网搜索的判断，使用的是 20 多个硬编码正则（/下载/, /试卷/, /高考/, /搜索/ 等），无法理解上下文和语义。

### 改动

**1. src/lib/deepseek.ts — 新增 determineSearchIntent()**
- 调用 DeepSeek 分析用户输入是否需要联网搜索
- 返回 `{ needsSearch, query, reason }` 结构化结果
- 含详细 prompt，明确需要/不需要搜索的场景
- 低温（0.1）确保判断稳定
- 失败时返回 fallback（needsSearch=false）

**2. src/app/api/chat/route.ts — 替换正则搜索检测**
- 导入 `determineSearchIntent` 
- 删除全部 20+ 个正则需要搜索的硬编码
- 用 `try { await determineSearchIntent(lastUserMsg) } catch { /* fallback regex */ }` 替代
- 搜索 query 优先用 DeepSeek 提取的结果
- DeepSeek 提取不到 query 时降级到原正则提取逻辑

### 数据流
```
用户消息 → determineSearchIntent(DeepSeek)
            ├─ needsSearch=true → query → deepSearch → 增强消息
            └─ needsSearch=false → 直接走 AI 对话
失败时 → fallback regex（原行为）
```

### 下次继续
- 对话 session 自动保存到 chat_sessions 表（前端页面打开时 saveChatAsync）
- Supabase 数据库表迁移文件整理
- jiying 导航栏搜索功能


## 2026-07-17 导航栏改造：搜索 + 通知系统 ✅

### 差距4：导航栏缺少搜索功能
**新建文件:** src/components/JiyingSearch.tsx
- 导航栏搜索按钮（🔍），桌面端显示文字+快捷键，移动端仅图标
- 点击后弹出模态搜索框（Ctrl+K / Cmd+K 快捷键）
- 搜索范围：素材库、模板、作品集（当前为本地演示数据）
- 实时过滤结果，支持 ESC 关闭，点击外部关闭
- 深色主题（#1A1A2E）与即影导航栏一致

### 差距5：导航栏缺少通知系统
**新建文件:** src/components/NotificationBell.tsx
- 铃铛图标按钮，未读数红点徽章（>9 显示 "9+"）
- 点击弹出通知下拉面板，支持"全部已读"
- 3 类通知：审核结果（✅）、数据复盘（📊）、系统公告（🔔）
- 未读高亮 + 黄色圆点标记
- 点击外部关闭

### 修改文件
**src/app/jiying/layout.tsx**
- 导入 JiyingSearch + NotificationBell
- 导航栏右侧新增 \<JiyingSearch /\> 和 \<NotificationBell /\>
- 修复 typo：classNam → className

### 导航栏右侧最终布局
```
[🔍 搜索 ⌘K] [🔔] [● 42/50] [🥇 黄金] [⬆升级] [退出] [👤 用户名] [☰]
```

### 下次继续
- 导航权限控制（GAP-04）：访客/付费用户不同权限
- 订阅限制真实化（GAP-02）：canChat 返回真实限制
- Supabase 数据库表迁移文件

## 2026-07-17 实体店AI赋能：LLM即行业引擎 ✅

### 新增文件
**`src/app/api/daily-content/generate-templates/route.ts`**
- POST /api/daily-content/generate-templates
- 输入：用户描述生意（自然语言）
- AI自动识别行业类型+子类，生成3条匹配的爆款视频方案
- 每条方案含：名称、钩子、脚本、节奏、时长、适合平台、合规预警
- 返回合规检查报告（高风险词、平台规则、建议）
- DeepSeek 不可用时自动降级到3个通用兜底模板

### 修改文件
**`src/components/DailyContentEngine.tsx`**
- 新增 7 个 state 变量（businessDesc, industryType, generatedTemplates 等）
- 新增 2 个处理函数（handleGenerateTemplates, handleSelectAndGenerate）
- 新增实体店模式 UI 区域（textarea输入 + 生成按钮 + 模板列表 + 合规预警）
- 用户偏好持久化到 localStorage（记录历史选择的模板类型，后续生成更精准）
- 整体置于原有的赛道/平台参数面板之前

### 用户完整流程
```
打开即影 → 输入"我是开火锅店的，人均80，手工炒料"
         ↓
AI 识别行业：餐饮→火锅
         ↓
生成 3 条方案：[3秒美食暴击] [老板故事] [价格反差]
         ↓
用户选择一条 → 生成完整文案 → 合规审核通过 → 视频生成 → 下载发布
```

### 核心设计原则
- 不需要预设行业数据库，LLM 即行业引擎（覆盖所有行业）
- 不需要用户上传视频，描述即素材（降低使用门槛）
- 用户偏好自动学习（越用越精准）
- 合规审核内置（避免限流风险）
- 3条兜底模板在 API 不可用时自动使用

